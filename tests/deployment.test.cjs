const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs/promises'),path=require('node:path'),os=require('node:os');
const ROOT=path.resolve(__dirname,'..');const load=()=>import('../scripts/prepare-pages.mjs');async function fixture(){const {RUNTIME}=await load();const dir=await fs.mkdtemp(path.join(os.tmpdir(),'luma-pages-'));for(const name of [...RUNTIME,'release.json']){await fs.mkdir(path.dirname(path.join(dir,name)),{recursive:true});await fs.copyFile(path.join(ROOT,name),path.join(dir,name));}return dir;}
test('Release catalog validates every runtime file, relative manifest and correct version',async()=>{const {validate,VERSION}=await load();const r=await validate(ROOT);assert.equal(r.version,VERSION);assert.equal(Object.keys(r.files).length,17);});
test('Pages artifact contains index.html at top level, with no duplicate docs or repository files',async()=>{const dir=await fixture();try{const {prepare}=await load();await fs.writeFile(path.join(dir,'README.md'),'not a website file');await fs.mkdir(path.join(dir,'docs'));await fs.writeFile(path.join(dir,'docs','index.html'),'old version');const result=await prepare(dir);assert.equal(result.files,18);const html=await fs.readFile(path.join(result.path,'index.html'),'utf8');assert(html.includes('name="app-version" content="5.2.0"'));await assert.rejects(fs.stat(path.join(result.path,'docs')));await assert.rejects(fs.stat(path.join(result.path,'README.md')));}finally{await fs.rm(dir,{recursive:true,force:true});}});
test('An empty checkout fails early with an actionable message',async()=>{const dir=await fs.mkdtemp(path.join(os.tmpdir(),'luma-empty-'));try{const{prepare}=await load();await assert.rejects(prepare(dir),/release.json ontbreekt/);}finally{await fs.rm(dir,{recursive:true,force:true});}});
test('Missing journey runtime cannot produce a new Pages artifact',async()=>{const dir=await fixture();try{const{validate}=await load();await fs.rm(path.join(dir,'assets','journey.js'));await assert.rejects(validate(dir),/Ontbrekend bestand: assets\/journey.js/);}finally{await fs.rm(dir,{recursive:true,force:true});}});
test('CI is read-only and Pages publishing does not require Jekyll',async()=>{const yaml=await fs.readFile(path.join(ROOT,'.github','workflows','check.yml'),'utf8');assert(yaml.includes('branches: [main, luma-5.2-journey-map]'));assert(yaml.includes('workflow_dispatch:'));assert(yaml.includes('contents: read'));assert(!yaml.includes('contents: write'));assert(!yaml.includes('pages: write'));assert(yaml.includes('node --test tests/*.test.cjs'));assert((await fs.stat(path.join(ROOT,'.nojekyll'))).isFile());});
